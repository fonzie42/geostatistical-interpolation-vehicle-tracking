# Python
__pycache__/
*.pyc
.venv/
venv/

# Raw data (1 GB, obtain separately; see data/README.md)
*.parquet

# Scratch
preprocessing/output/*.tmp
.DS_Store

# Figures regenerated by scripts at runtime. The committed copies live in figures/
# (exactly the images used in the thesis .tex). Scripts write into results/ and analysis/.
results/*.png
results/*.svg
analysis/*.png
